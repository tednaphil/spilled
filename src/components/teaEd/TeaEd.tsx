import { TeaHardcoded } from '../../utils/interface';

interface Props {
    name: string,
    tea: TeaHardcoded,
    slug: string,
    key: number,
}

function TeaEd({ name, tea, slug, key}: Props) {
    return (
        <></>
    )
}

export default TeaEd